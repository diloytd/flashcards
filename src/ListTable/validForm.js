export default function ValidForm (english, transcription, russian, setValid) {
    if(english == "" || transcription == "" || russian == ""){
        return setValid = true
    }
        return setValid = false
}